appId: app.pluct
---
# Pluct-Maestro-Flow-27UX-13RateLimit-01Validation.yaml
# Validates UX Improvement #3: Better rate limit messaging with retry timing
# Follows naming: [Project]-[Maestro]-[Flow]-[Category]-[Feature]-[Sequence][Validation]

- launchApp
- waitForAnimationToEnd

# Note: Rate limit requires 10+ requests in an hour to trigger
# This test structure validates the dialog would appear with improved messaging
# Actual rate limit testing requires pre-conditioning (10+ requests)

# Validate app is ready (rate limit dialog would appear on 11th request)
- assertVisible:
    text: "Pluct"
    optional: true
