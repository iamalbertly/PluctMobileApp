<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for app functionality -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Removed legacy storage permissions; use scoped storage if needed -->

    <application
        android:name=".PluctApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pluct"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <activity
            android:name=".PluctUIMain01ActivitySimple"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Pluct"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link for ingest URLs -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="pluct" android:host="ingest" />
            </intent-filter>
            <!-- Debug deep link for error testing -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pluct" android:host="debug" android:path="/error" />
            </intent-filter>
        </activity>
        
        <!-- Debug broadcast receiver for error testing -->
        <receiver
            android:name=".debug.ErrorTriggerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.pluct.action.TRIGGER_ERROR" />
            </intent-filter>
        </receiver>
        
        <!-- Share activity for handling ACTION_SEND intents -->
        <activity
            android:name=".share.PluctShareIngestActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Pluct"
            android:label="@string/share_activity_label"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        
        <!-- Web transcript activity for automated transcript capture -->
        <activity
            android:name=".web.WebTranscriptActivity"
            android:exported="false"
            android:theme="@style/Theme.Pluct"
            android:label="Auto Transcribe"
            android:screenOrientation="portrait" />
    </application>

</manifest>
